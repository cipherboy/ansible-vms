---
# Creates a development environment

- name: Install useful packages
  dnf:
    name: "{{ packages }}"
    state: installed
  vars:
    packages:
     - ccache
     - cmake
     - gdb
     - git
     - make
     - nmap
     - rpm-build
     - screen
     - tmux
     - vim-enhanced
